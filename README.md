# 💪 Kropp Fitness — Лендинг начального уровня

Адаптивный одностраничный сайт (лендинг) для фитнес-клуба. Проект создан для портфолио с целью демонстрации навыков чистой верстки, работы с препроцессором SCSS и модульной архитектуры стилей.

---

## 🛠️ Стек технологий и инструменты
* **HTML5** — семантическая разметка.
* **SCSS (Sass)** — препроцессор для стилизации (использование модулей `@use`/`@forward`, переменных, миксинов и вложенности).
* **BEM (БЭМ)** — методология именования классов для поддержки и масштабируемости кода.
* **WebStorm** — интегрированная среда разработки (IDE).
* **Sass File Watcher** — для автоматической локальной компиляции SCSS в CSS.
* **Git & GitHub** — контроль версий и хостинг проекта.

---

## 📂 Архитектура SCSS стилей
В проекте реализована модульная структура стилей по принципу «от общего к частному»:
* `styles/` — общая папка стилей.
    * `0-globalstyles/` — глобальные настройки: сброс стилей (normalize), шрифты, CSS-переменные. Папка собирается через `_index.scss` и пробрасывается дальше с помощью `@forward`.
    * `header/` — стили для шапки сайта. итд 
    * `styles.scss` — главный файл-диспетчер, собирающий все модули воедино.


## 📝 Дневник разработки (Changelog)

<details>
<summary>
 Этап 1: Подготовка проекта, настройка SCSS и базовые стили
</summary>

На данном этапе выполнена полная подготовка проекта к работе, настроена удобная структура папок и заложен фундамент для будущей верстки.

* **Настройка автоматической сборки стилей**:
    * Настроена автоматическая компиляция SCSS в обычный CSS прямо во время сохранения файлов в WebStorm.
    * Создан главный файл-диспетчер `styles.scss`, собирающий все стили воедино.

* **Организация структуры папок**:
    * Все глобальные стили вынесены в отдельную папку `0-globalstyles`.
    * Использованы современные директивы `@use` и `@forward` для аккуратного подключения файлов друг к другу без конфликтов в коде.

* **Подключение сброса стилей (`_normalize.scss`)**:
    * Интегрирован современный сброс стилей, устраняющий стандартные различия в отображении элементов между браузерами.
    * Обеспечена адаптивность картинок по умолчанию и настроена плавная прокрутка страницы.

* **Подключение шрифтов и настройка текста (`_fonts.scss`, `globals.scss`)**:
    * Локально подключены шрифты **Heebo** (для обычного текста) и **Yantramanav** (для заголовков).
    * Заданы базовый размер текста, комфортный межстрочный интервал (line-height) и автоматический перевод всех заголовков в верхний регистр.

* **Создание глобальных переменных (`_variables.scss`, `globals.scss`)**:
    * В `:root` вынесены все ключевые параметры: цвета проекта (черный, белый, серые оттенки), радиусы скругления углов, рамки и время анимации для удобного изменения темы в одном месте.
    * Создан класс ограничивающего контейнера `.container` (максимальная ширина контента 1300px с боковыми отступами по 15px), предотвращающий прилипание элементов к краям экрана.

* **Порядок в репозитории**:
    * Настроен файл `.gitignore` для исключения системных файлов (папка `node_modules`, настройки WebStorm и отчеты Repomix) из отслеживания Git.

</details>


<details>
<summary>
Этап 2: Разметка и стилизация адаптивной шапки (Header) и интеграция JS
</summary>

На данном этапе полностью завершена работа над шапкой сайта (Header): созданы разметка, стили, анимации, адаптивный оверлей и подключен интерактивный JavaScript для мобильной версии.

* **Семантическая разметка (HTML5 & BEM)**:
    * Написана разметка шапки с использованием логотипа, навигации по сайту и целевой кнопки «Book now».
    * Мобильное меню заверстано с использованием современного тега `<dialog>` для лучшей доступности.
    * Интегрирована кастомная SVG-кнопка бургера с подготовленными путями для анимации.

* **Интерактивный JavaScript (ES-Modules)**:
    * Написан масштабируемый класс `OverlayMenu` в виде отдельного модуля.
    * Реализовано переключение состояния кнопки бургера (`is-active`), открытие окна `<dialog>` и блокировка скролла всей страницы при открытом меню путем добавления класса `is-lock` на тег `html`.
    * Скрипт подключен как модуль (`type="module"`) в `<head>` документа.

* **Анимации и стилизация шапки (`_header.scss`, `_burger-button.scss`, `_button.scss`)**:
    * Шапка зафиксирована при прокрутке экрана (`position: sticky`, `top: 0`).
    * С помощью современного CSS (`animation-timeline: scroll()`) реализовано плавное уменьшение высоты шапки при прокрутке страницы вниз.
    * Реализовано плавное появление полноэкранного мобильного меню справа налево через свойства `opacity`, `translate` и современное правило `@starting-style` с поддержкой `transition-behavior: allow-discrete`.
    * Настроена красивая CSS-анимация превращения бургера в крестик при клике за счет смещения параметров `stroke-dasharray` и `stroke-dashoffset` у SVG-путей.
    * Вынесена стилизация кнопок в отдельные переиспользуемые модули.

</details>


<details>
<summary>
Этап 3: Верстка первого экрана (Banner), оптимизация утилит и интерактивная пагинация
</summary>

На данном этапе завершена разработка первого экрана (Banner): создана семантическая разметка контента, настроена гибкая пагинация и оптимизирована система глобальных SCSS-миксинов.

* **Разметка и стилизация баннера (`_banner.scss`, `index.html`)**:
    * Секция `.banner` спозиционирована на всю высоту экрана за вычетом высоты шапки с использованием динамических единиц `dvh`: `min-height: calc(100dvh - 70px)`.
    * Реализовано вертикальное выравнивание контента баннера к нижней части экрана с использованием флексбокс-структуры.
    * Интегрирован сверхкрупный заголовок с адаптивным расчетом размера шрифта: `font-size: min(360px, 18.75vw)`.
    * Добавлен декоративный элемент стрелки-указателя, адаптивно масштабируемый под разные разрешения экрана.

* **Адаптивная пагинация и эффекты наведения**:
    * Сверстана пагинация слайдера баннера. Интерактивные точки реализованы через псевдоэлементы `::after` с плавной сменой состояний.
    * Настроена правильная логика взаимодействия: наведение происходит на кнопку-родителя, после чего перекрашивается внутренний псевдоэлемент `::after`. Эффект применен только к неактивным точкам (`:not(.is-current)`).

* **Глобальные миксины доступности (`_maxins.scss`, `_utils.scss`)**:
    * Создан файл `_maxins.scss` и внедрен профессиональный миксин `@mixin visually-hidden` для скрытия элементов разметки с экрана без потери их доступности для программ экранного доступа (скринридеров).
    * Внедрен служебный класс `.visually-hidden` для скрытия вспомогательных заголовков секций (`h1`, `h2`).
    * Файлы утилит успешно интегрированы в общую сборку через центральный `0-globalstyles/_index.scss`.

</details>


<details>
<summary>
 Этап 4: Разработка секции Motivation (шахматная сетка, декоративные подложки и оптимизация производительности)
</summary>

На данном этапе реализована контентная секция Motivation, настроена сложная адаптивная сетка карточек, внедрены кастомные декоративные элементы и устранена проблема скачков интерфейса при прокрутке.

* **Шахматное позиционирование элементов (`_motivation.scss`, `index.html`)**:
    * Реализована сетка карточек мотивации на основе флексбокс-модели.
    * Применен принцип контекстного позиционирования: зеркальный разворот карточек реализован через обращение к порядковому номеру родительского элемента (`.motivation__item:nth-child(even) .motivation__card { flex-direction: row-reverse }`). Это позволило сохранить независимость и чистоту дочернего класса `.motivation__card`.

* **Декоративные элементы и доступность (`_backdrop-title.scss`, `_title-medium.scss`)**:
    * Внедрен переиспользуемый класс `.backdrop-title` для создания крупных фоновых текстовых подложек позади заголовков.
    * Текстовая подложка реализована через псевдоэлемент `::after` и CSS-функцию `content: attr(data-title)`. Это позволило не перегружать HTML-код лишними тегами, сохранив семантическую чистоту разметки и доступность для скринридеров (декоративный текст игнорируется программами чтения экрана).
    * Псевдоэлементу `::after` задано абсолютное позиционирование, что автоматически перевело его тип отображения в блочный без необходимости явно прописывать свойство `display: block`.
    * Элемент-маркер (плюс) привязан к размеру шрифта заголовка с использованием относительных единиц `em` для сохранения идеальных пропорций при масштабировании текста.

* **Разработка прозрачной интерактивной кнопки**:
    * Стилизована прозрачная кнопка `.button--transparent` со встроенной стрелкой в псевдоэлементе `::after`.
    * Реализована правильная иерархия наведения: событие hover обрабатывается на самой кнопке, плавно перекрашивая внутренний псевдоэлемент (`&::after { filter: brightness(0.75) }`).

* **Оптимизация скролла (Устранение скачков)**:
    * Найдена и устранена проблема резких скачков страницы при скролле вниз с баннера. Отрицательные вертикальные отступы картинок карточек (`margin-block: -60px`) заменены на безопасный сдвиг через `transform: translateY()`, что стабилизировало расчет высоты страницы браузером.

</details>


<details>
<summary>
Этап 5: Разработка секции Training Types (CSS Grid сетка, управление адаптивными ресурсами через CSS-переменные)
</summary>

На данном этапе разработана контентная секция видов тренировок (Training Types): создана гибкая Grid-сетка с трехступенчатой адаптивностью и оптимизировано управление фоновыми ресурсами при помощи локальных CSS-переменных.

* **Адаптивная CSS Grid сетка (`_training-types.scss`, `index.html`)**:
    * Список видов тренировок реализован на базе технологии CSS Grid с автоматическим распределением свободного пространства.
    * Реализована плавная трехступенчатая адаптивность: 6 колонок на широкоформатных экранах, автоматическая перестройка в 3 колонки на ноутбуках (`@include laptop`) и в 2 колонки на мобильных устройствах (`@include mobile`).

* **Оптимизация фоновых изображений через локальные переменные**:
    * Декоративные угловые стрелки интегрированы как фоновое изображение элементов списка (`__item`).
    * Для управления пространством под стрелку внедрена локальная CSS-переменная `--trainingTypesItemBgIconSize` со значением по умолчанию `80px`.
    * При адаптации под мобильные экраны изменено только значение этой переменной (`--trainingTypesItemBgIconSize: 60px`), что позволило избежать повторного дублирования длинного пути `background: url(...)` в медиа-запросах.

</details>

<details>
<summary>
Этап 6: Разработка секции Join Us (интерактивный JS-видеоплеер, форма подписки и компонент ввода)
</summary>

На данном этапе разработана интерактивная секция призыва к действию (Join Us), интегрирован функциональный JS-видеоплеер, создана форма подписки, стилизован переиспользуемый компонент полей ввода (Input) и оптимизирован механизм центрирования адаптивного декора.

* **Интерактивный видеоплеер на JavaScript (ES-Modules & DOM)**:
    * Интегрирован видео-плеер с оптимизацией загрузки через `poster` и явным указанием пропорций (`width`/`height`) во избежание сдвига макета.
    * Написан масштабируемый класс-модуль `VideoPlayer` для управления медиа-ресурсами. По клику на кнопку запуска воспроизведения запускается метод `.play()`, динамически активируются стандартные элементы управления браузера (`controls = true`) для удобной перемотки и регулировки звука, а сама кнопка Play плавно исчезает.
    * Исчезновение кнопки Play реализовано через плавное изменение прозрачности и свойство `visibility: hidden` (класс `.is-hidden`), что полностью исключает ложные клики по скрытой кнопке поверх работающего видео.
    * Класс инициализируется внутри обработчика `DOMContentLoaded` в файле `main.js` для гарантированной доступности DOM-дерева в момент старта скрипта.
    * Кнопке Play задан эффект «матового стекла» (glassmorphism) при помощи полупрозрачного фона и фильтра размытия элементов заднего плана (`backdrop-filter: blur(8px)`).

* **Горизонтальное центрирование инлайнового декора**:
    * Заголовок `.join-us__title` центрирует текст и псевдоэлемент-стрелку как единую строку за счет свойства `text-align: center` и перевода `::after` в тип отображения `display: inline-block`.
    * Использование относительных единиц `em` позволило стрелке масштабироваться синхронно с динамическим размером шрифта заголовка (`clamp()`).

* **Разработка формы и компонента ввода (`_join-us.scss`, `_input.scss`)**:
    * Структура формы семантически разделена на информационную шапку (`<header>`) и тело со списками полей.
    * Стилизован универсальный независимый компонент `.input` с плавными переходами состояний при наведении и фокусе (инверсия цветов для улучшения контраста при вводе).
    * Полям добавлены атрибуты `autocomplete="name"` и `autocomplete="email"` для корректной работы автозаполнения в браузерах.

* **Модификатор центрирования декора (`_backdrop-title.scss`)**:
    * Создан модификатор `.backdrop-title--center`, центрирующий фоновую подложку `::after` по оси X при помощи свойств `left: 50%` и `translate: -50% 0`, что позволило использовать этот эффект для центрированных заголовков форм.

</details>


<details>
<summary>
Этап 7: Разработка секции Location (интеграция интерактивной карты Google Maps и настройки безопасности)
</summary>

На данном этапе разработана географическая секция филиалов (Location): интегрирована интерактивная карта Google Maps с адаптивным поведением и настроены современные политики безопасности для работы со сторонними ресурсами.

* **Интеграция интерактивной карты Google Maps (`_location.scss`, `index.html`)**:
    * Внедрен интерактивный фрейм `<iframe>` вместо статичного изображения, предоставляющий пользователям доступ к полноценной карте в режиме реального времени.
    * Для `<iframe>` заданы жесткие пропорции (`width`/`height`), предотвращающие скачки макета (Layout Shifts) при загрузке страницы.
    * Настроен атрибут `referrerpolicy="strict-origin-when-cross-origin"`, защищающий конфиденциальность данных пользователей путем обрезания адреса Referrer до имени домена при запросах к серверам Google.

* **Адаптивная Grid-сетка и стилизация**:
    * Секция разделена на две колонки через CSS Grid (`repeat(2, 1fr)`) с перестроением в одну колонку на экранах ноутбуков и планшетов (`@include laptop`).
    * Для карты задана адаптивная высота на основе функции `clamp(400px, 31.25vw, 600px)`, гарантирующая удобство использования карты как на широкоформатных экранах, так и на смартфонах.
    * Карта стилизована скруглением углов в общей стилистике проекта.

* **Оптимизация кода**:
    * В HTML-разметке кнопки «view more» удален невалидный для тега ссылки `<a>` атрибут `type="button"`.
    * Успешно переиспользован глобальный класс типографики `.title-medium` и центрирующий модификатор `.backdrop-title--center`.

</details>


<details>
<summary>
Этап 8: Разработка секции Family (нативный горизонтальный CSS-слайдер, оптимизация адаптивной сетки карты)
</summary>

На данном этапе разработана визуальная секция фотогалереи (Family): создан нативный горизонтальный слайдер с эффектом прилипания слайдов, а также проведена финальная оптимизация адаптивного поведения блоков в секции расположения филиалов.

* **Разработка горизонтального слайдера на CSS (`_family.scss`, `index.html`)**:
    * Создана галерея изображений с нативным горизонтальным скроллом без привлечения сторонних JS-библиотек (оптимизация веса страницы).
    * Интегрирована технология CSS Scroll Snap (`scroll-snap-type: x mandatory` и `scroll-snap-align: center`) для обеспечения плавного автоматического «долистывания» и фиксации картинок по центру экрана при прокрутке.
    * Задано свойство `justify-content: start` для флекс-контейнера, что гарантирует точный запуск прокрутки строго с первого слайда без смещения.
    * Изображениям задано свойство `object-fit: cover` для сохранения правильных пропорций лиц при масштабировании, а также настроено скругление углов в соответствии с общей стилистикой проекта.

* **Адаптация под мобильные устройства**:
    * На мобильных экранах высота галереи зафиксирована на значении `360px` для обеспечения компактности и удобства вертикального скролла страницы.

* **Оптимизация сетки в секции Location (`_location.scss`)**:
    * Решена проблема некорректного порядка блоков при адаптации. Использование несистематичного свойства `order` заменено на жесткое позиционирование строк Grid-сетки: `grid-row: 1` для контента и `grid-row: 2` для интерактивной карты в медиа-запросе `@include laptop`. Это гарантирует, что на планшетах и телефонах текст всегда будет находиться сверху, а карта под ним.

</details>


<details>
<summary>
 Этап 9: Интерактивный фитнес-калькулятор (CSS Grid форма, валидная таблица и JS-вычисления TDEE/BMI)
</summary>

На данном этапе разработана интерактивная секция калькулятора (Calculate): создана семантическая форма для сбора данных, сверстана валидная таблица результатов и интегрирована интеллектуальная JS-логика расчетов на базе формулы Миффлина-Сан Жеора.

* **Семантическая разметка и таблица результатов (`_calculate.scss`, `index.html`)**:
    * Таблица результатов BMI полностью переведена на строгие стандарты HTML5 с использованием тегов `<table>`, `thead`, `tbody`, `tfoot` и строчным оборачиванием ячеек через `<tr>`/`<td>`/`<th>` для кроссбраузерной стабильности.
    * Реализован декоративный центрированный фоновый круг под таблицей при помощи абсолютно позиционированного псевдоэлемента `::before` с адаптивным диаметром `clamp(500px, 31.77vw, 610px)`.

* **Разработка Grid-формы и стилизация элементов ввода (`_input.scss`)**:
    * Сетка формы построена на CSS Grid (`repeat(2, 1fr)`) с адаптивным перестроением в одну колонку на экранах мобильных устройств.
    * Для полей выбора `<select>` осуществлен сброс системных стрелочек через кроссбраузерное свойство `appearance: none`. Взамен интегрирована аккуратная кастомная векторная стрелочка в виде фонового изображения.
    * Цвет текста неактивных пунктов-заглушек в выпадающих списках синхронизирован с цветом плейсхолдеров инпутов при помощи CSS-псевдокласса `:invalid`. При выборе реального значения цвет текста плавно переключается в белый (`:valid`).
    * Для числовых полей ввода (`input[type="number"]`) применен безопасный сброс дефолтных стрелочек-спиннеров во всех популярных браузерах (Blink/WebKit и Firefox) без блокировки возможности ввода данных с клавиатуры.

* **Интерактивная JS-логика расчетов (Разработано при поддержке ИИ)**:
    * В сотрудничестве с искусственным интеллектом спроектирован и интегрирован JS-модуль `BMRCalculator.js` для динамического вычисления фитнес-показателей.
    * Реализован автоматический расчет базового метаболизма (BMR) по формуле Миффлина-Сан Жеора, который затем умножается на выбранный коэффициент активности (TDEE).
    * Программа автоматически вычисляет индекс массы тела (BMI) и выводит его статус, а также динамически генерирует персонализированные рекомендации (Fit Tip) в зависимости от весовой категории пользователя.
    * Расчет происходит мгновенно по клику на кнопку «Calculate» без перезагрузки веб-страницы.

</details>


<details>
<summary>
### Этап 10: Разработка адаптивного футера (CSS Grid сетка, семантическая разметка и CSS-микроинтеракции)
</summary>

На данном этапе полностью завершена разработка подвала сайта (Footer): создана семантическая разметка, настроена сложная адаптивная Grid-сетка с брейкпоинтами и интегрированы плавные CSS-эффекты для интерактивных элементов.

* **Семантическая разметка (HTML5 & Accessibility)**:
    * Для блока контактов применен правильный тег `<address>`.
    * Время работы и год в копирайте обернуты в семантические теги `<time>` с указанием атрибута `datetime` для поисковых роботов.
    * Ссылкам социальных сетей добавлены скрытые текстовые описания (`visually-hidden`) для доступности скринридерами.

* **Сложная Grid-архитектура подвала (`_footer.scss`, `index.html`)**:
    * Верхняя часть подвала (`__header`) разделена на две колонки: под логотип (`20.77%`) и под описание (`auto`) с автоматическим перестроением в одну колонку на мобильных.
    * Основное тело подвала (`__body`) выстроено в четырехколончатую Grid-сетку. Реализована плавная адаптивность: на планшетах (`@include tablet`) сетка перестраивается в 2 колонки, а на телефонах (`@include mobile-s`) — в 1 колонку.

* **Интерактивные CSS-микроинтеракции**:
    * Реализована плавная инверсия цвета кнопки-стрелочки в черный при клике (фокусе) на поле ввода почты при помощи соседнего селектора и фильтра (`&:focus + .footer__subscribe-button { filter: invert(1) }`).
    * Для кнопки подписки добавлен тактильный эффект нажатия (кнопка плавно сжимается на 10% через `scale: 0.9` при состоянии `:active`).
    * Векторным иконкам социальных сетей задано значение `currentColor` для плавной смены цвета при наведении на ссылки в SCSS.

</details>